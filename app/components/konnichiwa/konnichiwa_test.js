'use strict';

describe('converse.konnichiwa module', function() {

  beforeEach(module('converse.konnichiwa'));

  describe('konnichiwa controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var konnichiwaController = $controller('KonnichiwaController');
      expect(konnichiwaController).toBeDefined();
    }));

  });
});